# Factors and Multiples

import { Card, Cards } from 'nextra-theme-docs'
import units from 'config/units'

## भाग
{units.map(
    (module, index) =>
    index == 2 && (
        <Cards>
            {module.units.map(
                (item, index) =>
                item.unit && (
                    <Card title={item.unit} href={module.slug + "/" + item.key} key={index} />
                )
            )}
        </Cards>
    )
)}